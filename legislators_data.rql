legislators_data(state: string := "%", type: string := "%", district: int := 100) := {
 SELECT * FROM READ("https://theunitedstates.io/congress-legislators/legislators-current.csv") as l
 WHERE
  l.state like state AND
  l.type like type AND
  l.district < district
 order by l.district
 LIMIT 10
}